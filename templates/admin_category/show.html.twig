{% extends 'base.html.twig' %}

{% block title %}Les pays{% endblock %}

{% block body %}
    <h1>Pays</h1>

    <table class="table">
        <tbody>
         
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_category_index') }}">retour à la liste des pays</a>

    <a href="{{ path('app_admin_category_edit', {'id': category.id}) }}">Modifier</a>

    {{ include('admin_category/_delete_form.html.twig') }}
{% endblock %}
